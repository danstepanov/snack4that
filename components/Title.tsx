const Title = ({ children }: { children: string}) => (
    <h1 className="animate-move-bg bg-gradient-to-r from-indigo-500 via-pink-500 to-indigo-500 bg-400% bg-clip-text md:text-5xl text-4xl font-bold text-transparent">{children}</h1>
)

export default Title